# PostgreSQL
# Use "localhost" when running outside Docker, "postgres" when inside Docker
DATABASE_URL=postgres://eventpulse:eventpulse_dev@localhost:5432/eventpulse

# Redis
# Use "localhost" when running outside Docker, "redis" when inside Docker
REDIS_URL=redis://localhost:6379

# Server
HOST=0.0.0.0
PORT=3000
NODE_ENV=development

# Retention (worker startup cleanup — best-effort, local dev only)
# Set to 0 to disable cleanup for either table.
EVENT_RETENTION_DAYS=30
ANOMALY_RETENTION_DAYS=90
